stages:
  - build
  - test
  - docker
  - deploy

build-job:    
  stage: build
  script:
    - gradle assemble
  tags:
    - shared

unit-test-job:
  stage: test
  script:
    - gradle check
  tags:
    - shared

docker-job:
  stage: docker
  script:
    - docker info
    - docker build .
  tags:
    - shared

deploy-job:
  stage: deploy
  environment: production
  script:
    - echo "Deploying application..."
    - echo "Application successfully deployed."
  tags:
    - shared
